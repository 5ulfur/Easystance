const config = {
    apiUrl: 'http://127.0.0.1:5001',
    endpoints: {
        login: '/auth/login',
        logout: '/auth/logout',
        checkAuth: '/auth/check'
    }
};
  
export default config;